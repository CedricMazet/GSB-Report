{% extends "layout.html.twig" %}
{% set drugMenu = true %}

{% block title %}Recherche de praticiens{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
<div class="well">
    <form class="form-horizontal" role="form" action="/practitioners/results/" method="post">
        <div class="form-group">
            <label class="col-sm-2 col-sm-offset-1 control-label">Type Praticiens</label>
            <div class="col-sm-7">
                <select class="form-control" name="practitionerType">
                    {% for practitionertype in practitionerTypes %}
                        <option value="{{ practitionertype.id }}">{{ practitionertype.name }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-3 col-sm-offset-5">
                <button type="submit" class="btn btn-default btn-primary"><span class="glyphicon glyphicon-search"></span> Rechercher</button>
            </div>
        </div>
    </form>
</div>
{% endblock %}